namespace teleporter.integration.cluster.rpc.fbs.generate.instance;

enum Action:byte {
    ADD, UPDATE, UPSERT, REMOVE
}

table ConfigChangeNotify {
    key:string;
    action:Action;
    timestamp:long;
}

table HealthResponse {
    totalMemory:float;
    freeMemory:float;
}

table LogResponse {
    line:string;
}